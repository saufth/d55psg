// Types
import type { ButtonTypesList } from '@/types/input'

/** Button types configuration */
export const BUTTON_TYPES: Readonly<ButtonTypesList> = Object.freeze({
  button: 'button',
  submit: 'submit',
  reset: 'reset'
})
